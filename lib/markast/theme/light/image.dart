import 'package:flutter/material.dart';

const BorderRadius kBorderRadius = BorderRadius.all(Radius.circular(10));

const TextStyle kTitleText = TextStyle(
  color: Color(0xFF6B6E80),
  fontSize: 13,
  fontStyle: FontStyle.italic,
);

final BoxDecoration kPlaceholderDecoration = BoxDecoration(
  color: const Color(0xFFF4F4FA),
  border: Border.all(color: const Color(0xFFE0E0EA)),
  borderRadius: BorderRadius.circular(10),
);

const TextStyle kPlaceholderText = TextStyle(color: Color(0xFF6B6E80));
